# Zana MCP tool reference

Generated by `scripts/generate-mcp-reference.ts` from `packages/mcp/src/mcp-server.ts` and handler sources. Re-run after MCP changes:

    npm run docs:mcp-ref

This file is the source of truth for slash-command authors. Use the documented field names and enum values verbatim — do not guess.

Looking for end-to-end examples instead of per-tool signatures? See [`RECIPES.md`](RECIPES.md) — every recipe there is mirrored by a live Claude integration test under `scripts/qa/`.

---
## Index

- [`zana_agent_result`](#zana_agent_result)
- [`zana_agent_status`](#zana_agent_status)
- [`zana_artifact_create`](#zana_artifact_create)
- [`zana_artifact_list`](#zana_artifact_list)
- [`zana_artifact_read`](#zana_artifact_read)
- [`zana_artifact_update`](#zana_artifact_update)
- [`zana_ask_agent`](#zana_ask_agent)
- [`zana_autopilot_goal_cancel`](#zana_autopilot_goal_cancel)
- [`zana_autopilot_goal_driven`](#zana_autopilot_goal_driven)
- [`zana_autopilot_goal_list`](#zana_autopilot_goal_list)
- [`zana_autopilot_goal_status`](#zana_autopilot_goal_status)
- [`zana_channel_history`](#zana_channel_history)
- [`zana_check_inbox`](#zana_check_inbox)
- [`zana_checkpoint_get`](#zana_checkpoint_get)
- [`zana_checkpoint_list`](#zana_checkpoint_list)
- [`zana_checkpoint_resume`](#zana_checkpoint_resume)
- [`zana_checkpoint_save`](#zana_checkpoint_save)
- [`zana_delete_profile`](#zana_delete_profile)
- [`zana_delete_skill`](#zana_delete_skill)
- [`zana_delete_team`](#zana_delete_team)
- [`zana_deliberate`](#zana_deliberate)
- [`zana_deliberate_cancel`](#zana_deliberate_cancel)
- [`zana_deliberation_list`](#zana_deliberation_list)
- [`zana_deliberation_nudge`](#zana_deliberation_nudge)
- [`zana_deliberation_override`](#zana_deliberation_override)
- [`zana_deliberation_status`](#zana_deliberation_status)
- [`zana_discover_agents`](#zana_discover_agents)
- [`zana_event_emit`](#zana_event_emit)
- [`zana_event_query`](#zana_event_query)
- [`zana_get_profile`](#zana_get_profile)
- [`zana_get_skill`](#zana_get_skill)
- [`zana_get_team`](#zana_get_team)
- [`zana_kill_agent`](#zana_kill_agent)
- [`zana_list_agents`](#zana_list_agents)
- [`zana_list_channels`](#zana_list_channels)
- [`zana_list_profiles`](#zana_list_profiles)
- [`zana_list_running_teams`](#zana_list_running_teams)
- [`zana_list_skills`](#zana_list_skills)
- [`zana_list_teams`](#zana_list_teams)
- [`zana_memory_search`](#zana_memory_search)
- [`zana_memory_store`](#zana_memory_store)
- [`zana_module_config_get`](#zana_module_config_get)
- [`zana_module_config_list`](#zana_module_config_list)
- [`zana_module_config_set`](#zana_module_config_set)
- [`zana_oneshot_query`](#zana_oneshot_query)
- [`zana_plan_create`](#zana_plan_create)
- [`zana_publish_channel`](#zana_publish_channel)
- [`zana_route_task`](#zana_route_task)
- [`zana_save_profile`](#zana_save_profile)
- [`zana_save_skill`](#zana_save_skill)
- [`zana_save_team`](#zana_save_team)
- [`zana_schedule_create`](#zana_schedule_create)
- [`zana_schedule_delete`](#zana_schedule_delete)
- [`zana_schedule_disable`](#zana_schedule_disable)
- [`zana_schedule_enable`](#zana_schedule_enable)
- [`zana_schedule_get`](#zana_schedule_get)
- [`zana_schedule_list`](#zana_schedule_list)
- [`zana_schedule_reload`](#zana_schedule_reload)
- [`zana_schedule_trigger`](#zana_schedule_trigger)
- [`zana_schedule_update`](#zana_schedule_update)
- [`zana_send_ack`](#zana_send_ack)
- [`zana_send_message`](#zana_send_message)
- [`zana_spawn_agent`](#zana_spawn_agent)
- [`zana_spawn_agent_validated`](#zana_spawn_agent_validated)
- [`zana_sprint_board`](#zana_sprint_board)
- [`zana_sprint_create`](#zana_sprint_create)
- [`zana_sprint_end`](#zana_sprint_end)
- [`zana_sprint_list`](#zana_sprint_list)
- [`zana_sprint_start`](#zana_sprint_start)
- [`zana_start_team`](#zana_start_team)
- [`zana_stop_team`](#zana_stop_team)
- [`zana_subscribe_channel`](#zana_subscribe_channel)
- [`zana_swarm_broadcast`](#zana_swarm_broadcast)
- [`zana_swarm_instruct`](#zana_swarm_instruct)
- [`zana_swarm_list`](#zana_swarm_list)
- [`zana_swarm_poll_events`](#zana_swarm_poll_events)
- [`zana_swarm_spawn`](#zana_swarm_spawn)
- [`zana_swarm_stop`](#zana_swarm_stop)
- [`zana_team_status`](#zana_team_status)
- [`zana_ticket_add_to_sprint`](#zana_ticket_add_to_sprint)
- [`zana_ticket_claim`](#zana_ticket_claim)
- [`zana_ticket_comment`](#zana_ticket_comment)
- [`zana_ticket_complete`](#zana_ticket_complete)
- [`zana_ticket_create`](#zana_ticket_create)
- [`zana_ticket_edit`](#zana_ticket_edit)
- [`zana_ticket_get`](#zana_ticket_get)
- [`zana_ticket_list`](#zana_ticket_list)
- [`zana_ticket_update`](#zana_ticket_update)
- [`zana_ticket_update_status`](#zana_ticket_update_status)
- [`zana_toggle_skill`](#zana_toggle_skill)
- [`zana_workers_list`](#zana_workers_list)
- [`zana_workflow_get_run`](#zana_workflow_get_run)
- [`zana_workflow_list_runs`](#zana_workflow_list_runs)
- [`zana_workflow_run`](#zana_workflow_run)
---

## zana_agent_result

**Description:** Get the result/output of a completed agent. Returns null if agent is still running.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| agentId | string | yes | Agent ID to get results from |

**Output shape:**

```json
{
  "completed": true,
  "id": "agent-uuid",
  "result": "...",
  "state": "terminated"
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:171`
- Handler: `packages/core/src/agents/manager.ts case 'agent_result'`

**Common pitfalls:**

- `result` is `null` while the agent is still running. Check `completed` (boolean) before using `result`.

---

## zana_agent_status

**Description:** Get detailed status of a specific agent.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| agentId | string | yes | Agent ID to check |

**Output shape:**

```json
{
  "id": "agent-uuid",
  "lastAction": "...",
  "mode": "headless",
  "state": "active",
  "uptime": 12345
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:160`
- Handler: `packages/core/src/agents/manager.ts case 'agent_status'`

---

## zana_artifact_create

**Description:** Create a planning artifact (architecture doc, requirement spec, design doc, etc.) that is shared with all daemons. Use this to document plans, decisions, and specs before implementation.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| content | string | yes | Full markdown content of the artifact |
| linkedTickets | array<string> | no | IDs of related tickets |
| tags | array<string> | no | Tags for filtering |
| title | string | yes | Title of the artifact |
| type | string | yes | `architecture-doc` \| `requirement-spec` \| `design-doc` \| `api-contract` \| `runbook` \| `decision-record` \| `custom` — Type of planning artifact |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1078`

---

## zana_artifact_list

**Description:** List all shared planning artifacts. Returns metadata (id, title, type, tags) without full content.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| tag | string | no | Filter by tag |
| type | string | no | Filter by artifact type |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1093`

---

## zana_artifact_read

**Description:** Read the full content of a specific artifact by ID. Use this to access architecture docs, requirement specs, and other planning documents.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| artifactId | string | yes | The artifact ID to read |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1104`

---

## zana_artifact_update

**Description:** Update an existing artifact's content or metadata.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| artifactId | string | yes | ID of the artifact to update |
| content | string | no | New content (optional) |
| linkedTickets | array<string> | no | Updated linked ticket IDs (optional) |
| tags | array<string> | no | New tags (optional) |
| title | string | no | New title (optional) |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1115`

---

## zana_ask_agent

**Description:** Send a question to another agent (P2P). Questions are read-only — you cannot give instructions, only ask. The agent will see your question in their inbox on their next tool call.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| question | string | yes | Your question for the other agent |
| replyTo | string | no | Optional message ID if this is a reply |
| toAgentId | string | yes | Target agent ID (from zana_discover_agents) |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:778`

---

## zana_autopilot_goal_cancel

**Description:** Cancel a running goal.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| goalId | string | yes |  |

**Output shape:**

```json
{
  "ok": true
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1264`
- Handler: `packages/core/modules/autopilot/index.js:cancelGoal`

**Common pitfalls:**

- Already-terminal goals fail: `{ ok: false, error: 'goal already <status>' }`.

---

## zana_autopilot_goal_driven

**Description:** Start a goal-driven task that loops a sequence of agent steps until success criteria are met. Returns the goal ID immediately; run autopilot_goal_status to check progress.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| criteria | string | yes | Success conditions — what must be true for the goal to be 'done'. The evaluator agent judges these. |
| steps | array<object> | yes | Ordered list of agent invocations. Each step spawns one agent. On failure, the loop restarts from step 0 with feedback. |
| title | string | yes | Short title — what you want to achieve |

**Output shape:**

```json
{
  "goalId": "uuid",
  "status": "running"
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1223`
- Handler: `packages/core/modules/autopilot/index.js:setGoal`

**Common pitfalls:**

- Returns immediately with `{ goalId, status: 'running' }` — the goal loop runs async. Poll with `zana_autopilot_goal_status`.

---

## zana_autopilot_goal_list

**Description:** List all autopilot goals (filterable by status).

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| status | string | no | Filter by status: running, completed, failed, exhausted, cancelled |

**Output shape:**

```json
[
  {
    "createdAt": 1234567890,
    "id": "uuid",
    "iteration": 2,
    "status": "running",
    "title": "..."
  }
]
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1256`
- Handler: `packages/core/modules/autopilot/index.js:listGoals`

**Common pitfalls:**

- List entries are summaries — `criteria`, `steps`, `results`, `lastEvaluation` are NOT included. Use `zana_autopilot_goal_status` for the full record.

---

## zana_autopilot_goal_status

**Description:** Get the status of a goal-driven task by ID. Returns: status (running/completed/failed/exhausted/cancelled), iteration count, latest evaluation result.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| goalId | string | yes |  |

**Output shape:**

```json
{
  "createdAt": 1234567890,
  "criteria": "...",
  "id": "uuid",
  "iteration": 2,
  "lastEvaluation": "VERDICT: FAIL\nReason: ...",
  "results": [
    {
      "agentId": "uuid",
      "step": 0,
      "summary": "..."
    }
  ],
  "status": "running",
  "steps": [
    {
      "profile": "coder",
      "prompt": "..."
    }
  ],
  "title": "..."
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1247`
- Handler: `packages/core/modules/autopilot/index.js:getGoal`

**Common pitfalls:**

- On miss returns `{ error: 'unknown goalId' }` (the wrapper) — direct `getGoal` returns `null`.
- `status` is one of `running | completed | failed | exhausted | cancelled`.

---

## zana_channel_history

**Description:** Get recent message history from a channel.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| channel | string | yes | Channel name |
| limit | number | no | Max messages to return (default: 50) |

**Output shape:**

```json
[
  {
    "channel": "my-channel",
    "fromAgentId": "agent-uuid",
    "fromAgentName": "Agent",
    "fromDaemonId": "local",
    "id": "msg-uuid",
    "payload": {},
    "sentAt": 1234567890,
    "type": "..."
  }
]
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:861`
- Handler: `packages/swarm/src/swarm/router.ts:getChannelHistory (via case 'channel_history')`

**Common pitfalls:**

- Unknown channel returns `[]`, not an error.
- `limit` defaults to 50 in the MCP wrapper; the underlying router accepts no limit and returns the full ring buffer if not bounded.

---

## zana_check_inbox

**Description:** Explicitly check your inbox for P2P messages from other agents. Messages are also auto-appended to tool responses, but use this to check proactively.

**Input:**

_No input parameters._

**Output shape:**

```json
[
  {
    "fromAgentId": "agent-uuid",
    "fromAgentName": "Agent",
    "fromDaemonId": "local",
    "id": "msg-uuid",
    "payload": {},
    "priority": "normal",
    "replyTo": null,
    "requiresAck": false,
    "sentAt": 1234567890,
    "toAgentId": "self-terminal-id",
    "type": "..."
  }
]
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:791`
- Handler: `packages/swarm/src/swarm/router.ts:drainInbox (via case 'check_inbox')`

**Common pitfalls:**

- Returns a flat array. Empty inbox returns `[]`, not `{ messages: [] }`.
- Drains on read — calling twice yields the second result empty unless new messages arrived between calls.
- Uses `process.env.ZANA_TERMINAL_ID` as the inbox key; outside an agent context this is undefined and the call returns `[]`.

---

## zana_checkpoint_get

**Description:** Get full details of a specific checkpoint.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| checkpointId | string | yes | Checkpoint ID |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1015`

---

## zana_checkpoint_list

**Description:** List saved checkpoints, optionally filtered by teamId or status.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| status | string | no | `running` \| `completed` \| `stopped` \| `resumed` |
| teamId | string | no |  |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1004`

---

## zana_checkpoint_resume

**Description:** Resume a stopped or interrupted team run from a checkpoint. Re-spawns pending agents with context from completed ones.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| checkpointId | string | yes | Checkpoint ID to resume from |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1026`

---

## zana_checkpoint_save

**Description:** Manually save a checkpoint of the current team run state. Includes completed and pending agents.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| pendingAgents | array<object> | no | Agents that still need to run |
| teamId | string | yes | Team ID this checkpoint belongs to |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:979`

---

## zana_delete_profile

**Description:** Delete a user-created profile by ID. Built-in profiles cannot be deleted.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| profileId | string | yes | Profile ID to delete |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:239`

---

## zana_delete_skill

**Description:** Delete a Skill by ID.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| skillId | string | yes | Skill ID to delete |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:291`

---

## zana_delete_team

**Description:** Delete a team template by ID. Built-in templates re-seed on daemon restart unless their seed-marker tracks them as deleted.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| teamId | string | yes | Team ID to delete |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:606`

---

## zana_deliberate

**Description:** Start a multi-voice deliberation: parallel review → synthesis → up-to-N convergence rounds → settle or escalate. Each voter is an independent agent with its own profile/model. Dissent is preserved verbatim across EVERY round. Default behavior: returns IMMEDIATELY with the proposed deliberation record (state=PROPOSED, _outcome='running'). Real-Claude voters can take 5-10 min each; the orchestration loop runs detached on the daemon. Poll `zana_deliberation_status({deliberationId})` until state is SETTLED or ESCALATED. Pass `wait: true` to block until completion (legacy / scripted use). When complete, the deliberation record carries: `_outcome` — 'settled' | 'escalated' | 'escalated_at_assembly' | 'escalated_during_reassembly' | 'judged'. `_judge` — present only on 'judged'; { verdict, humanId } describing how the auto-judge resolved an escalation. `_judgeError` — present only when the configured strategy was judge/hybrid but adjudication failed; deliberation stays ESCALATED for manual override. `_assemblyEscalation` — present only on 'escalated_at_assembly'; { reason, details } describing why the initial council failed to convene (e.g. quorum_lost, all_probes_failed). `_reassemblyEscalation` — present only on 'escalated_during_reassembly'; { reason, details } describing why a subsequent round's council failed (e.g. dropout_was_dissenter).

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| context | array<string> | no | Optional artifact IDs to seed context. Each is read and embedded in the shared prompt. |
| escalationStrategy | string | no | `human` \| `judge` \| `hybrid` — Override the deliberation module's escalationStrategy for this call. 'judge' / 'hybrid' auto-resolve a non-high-risk ESCALATED outcome by spawning a judge agent that reads the transcript and emits a verdict. 'human' leaves the deliberation ESCALATED so an operator can call zana_deliberation_override. riskTag='high' always escalates to a human regardless of this setting. |
| humanNudge | ? | no | Pause the loop after synthesizing the named round and exit with _outcome='awaiting_nudge'. Resume via zana_deliberation_nudge with either a free-text response (injected into next round) or null/empty (skip). |
| mode | string | no | `synthesis` \| `tally` — Reduction mode (default from module config). |
| modelDiversity | string | no | `uniform` \| `mixed` — uniform (default) — every voter uses its profile's declared model. mixed — rotate through {claude-sonnet-4-6, claude-haiku-4-5, claude-opus-4-7} for monoculture-resistance. Per-voter overrides in voterModels still win over the rotation. |
| question | string | yes | What the council is deliberating on. |
| quorum | string \| number | no | majority \| all \| <integer>. Default from module config. |
| riskTag | string | no | `low` \| `medium` \| `high` — high → mandatory escalation regardless of vote (default 'medium'). |
| rounds | number | no | Hard cap on convergence rounds (default from module config). |
| voterModels | object | no | Per-voter model override: { profileId → modelId }. Replaces the profile's declared model for this deliberation only. Each model must be in the local registry. |
| voters | array<string> \| object | no | Voters to convene. Either an explicit profile-id array OR a named role pack { pack, quantity? }. Defaults to ['architect','security-reviewer','researcher'] when omitted. |
| wait | boolean | no | If true, block until the deliberation reaches a terminal state. Default false (async). |

**Output shape:**

```json
{
  "_assemblyEscalation": null,
  "_outcome": "settled",
  "_reassemblyEscalation": null,
  "createdAt": "ISO8601",
  "currentRound": 1,
  "escalationReason": null,
  "id": "uuid",
  "question": "...",
  "rounds": 2,
  "settledAt": "ISO8601",
  "state": "SETTLED",
  "updatedAt": "ISO8601",
  "verdict": {
    "...": "...",
    "decision": "approve"
  },
  "version": 7,
  "voters": [
    {
      "...": "...",
      "agentId": "uuid",
      "modelId": "opus",
      "profileId": "architect"
    }
  ]
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/tools/deliberate.ts:1077`
- Handler: `packages/mcp/src/tools/deliberate.ts:deliberateHandler`

**Common pitfalls:**

- Always inspect `_outcome` first: `settled | escalated | escalated_at_assembly | escalated_during_reassembly`. The latter two carry diagnostic detail under `_assemblyEscalation` / `_reassemblyEscalation`.
- States: `PROPOSED | REVIEWING | SYNTHESIZING | CONVERGING | SETTLED | ESCALATED | EXHAUSTED`. SETTLED and ESCALATED are terminal.

---

## zana_deliberate_cancel

**Description:** Cancel a running deliberation. Flips the kill switch on the in-process orchestration loop, terminates any spawned voter agents, and drives the deliberation toward EXHAUSTED. Returns immediately; the EXHAUSTED transition lands shortly after via the runner's cancellation handler. If the deliberation is already terminal (SETTLED/ESCALATED/EXHAUSTED), returns _alreadyTerminal=true with no mutation. If the run isn't tracked in this process (e.g. daemon restarted), best-effort transitions to EXHAUSTED and returns _orphan=true. Result includes `_outcome` ('cancelling' | 'already_terminal' | 'cancelled') and, for live cancels, `_killedAgents` count.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| deliberationId | string | yes |  |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/tools/deliberate.ts:1253`

---

## zana_deliberation_list

**Description:** List deliberation summaries, optionally filtered by state.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| state | string | no | `PROPOSED` \| `REVIEWING` \| `SYNTHESIZING` \| `CONVERGING` \| `SETTLED` \| `ESCALATED` \| `EXHAUSTED` — Filter by deliberation state. Omit to list all. |

**Output shape:**

```json
[
  {
    "createdAt": "ISO8601",
    "currentRound": 1,
    "escalationReason": null,
    "id": "uuid",
    "question": "...",
    "rounds": 2,
    "settledAt": "ISO8601",
    "state": "SETTLED",
    "updatedAt": "ISO8601",
    "verdict": {
      "...": "...",
      "decision": "approve"
    },
    "voters": 3
  }
]
```

**Source:**
- MCP wrapper: `packages/mcp/src/tools/deliberate.ts:1221`
- Handler: `packages/mcp/src/tools/deliberate.ts:deliberationListHandler`

**Common pitfalls:**

- `voters` here is a COUNT (number), not an array. Use `zana_deliberation_status` to get the full voter records.

---

## zana_deliberation_nudge

**Description:** Resume a paused deliberation by recording an optional human reviewer note. Pass `response` as a non-empty string to inject the text verbatim into the next round's voter prompts (recorded with contributedBy='user'). Pass null or an empty string to record a 'skip' and continue without changing prompts. Errors if the deliberation is not currently awaiting a nudge.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| deliberationId | string | yes |  |
| response | string \| null | no | Free-text human note. null or empty string = skip. |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/tools/deliberate.ts:1189`

---

## zana_deliberation_override

**Description:** Record a human override on a deliberation. Stores the reason in the content-addressed artifact store and stamps the reasonHash onto the deliberation. Lands the deliberation on SETTLED.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| decision | string | yes | `approve` \| `reject` \| `rework` |
| deliberationId | string | yes |  |
| humanId | string | no | Optional human id. Defaults to 'human'. |
| reason | string | yes |  |

**Output shape:**

```json
{
  "...": "...",
  "id": "uuid",
  "override": {
    "decision": "approve",
    "humanId": "human",
    "reasonHash": "sha256-hex",
    "ts": "ISO8601"
  },
  "state": "SETTLED",
  "verdict": {
    "...": "...",
    "decision": "approve",
    "reasonHash": "sha256-hex"
  },
  "version": 8
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/tools/deliberate.ts:1236`
- Handler: `packages/mcp/src/tools/deliberate.ts:deliberationOverrideHandler`

**Common pitfalls:**

- Always lands the deliberation on `SETTLED`, regardless of prior state.
- `reason` is content-addressed — the deliberation stores `reasonHash`, not the raw text.

---

## zana_deliberation_status

**Description:** Load a deliberation by id and return its full state record.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| deliberationId | string | yes |  |

**Output shape:**

```json
{
  "createdAt": "ISO8601",
  "currentRound": 0,
  "escalationReason": null,
  "id": "uuid",
  "question": "...",
  "rounds": 2,
  "settledAt": null,
  "state": "REVIEWING",
  "updatedAt": "ISO8601",
  "verdict": null,
  "version": 3,
  "voters": [
    {
      "...": "...",
      "agentId": "uuid",
      "profileId": "architect"
    }
  ]
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/tools/deliberate.ts:1209`
- Handler: `packages/mcp/src/tools/deliberate.ts:deliberationStatusHandler`

**Common pitfalls:**

- Throws (not returns `{ error }`) when the deliberation is missing.

---

## zana_discover_agents

**Description:** Discover agents across the entire swarm. Returns agent IDs, names, profiles, and which daemon they belong to. Use this to find agents you can ask questions to.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| query | string | no | Optional search filter (matches agent name, profile, ID) |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:768`

---

## zana_event_emit

**Description:** Emit a custom event to the swarm event bus. Other agents and UI can observe it.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| payload | object | no | Event data |
| tags | array<string> | no |  |
| type | string | yes | Event type (e.g. 'progress', 'milestone') |

**Output shape:**

```json
{
  "ok": true
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:738`
- Handler: `packages/core/src/events/service.ts:emit (via case 'event_emit')`

**Common pitfalls:**

- Fire-and-forget — the event is appended to the event log; subscribers fire synchronously but errors are swallowed.
- `source` is auto-stamped from `process.env.ZANA_TERMINAL_ID` (defaults to 'agent') — do not pass it from the caller.

---

## zana_event_query

**Description:** Query recent events from the swarm event bus.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| limit | number | no | Max events to return (default 50) |
| since | number | no | Timestamp (ms) — only events after this |
| source | string | no | Filter by source agent/daemon |
| types | array<string> | no | Filter by event types |

**Output shape:**

```json
[
  {
    "id": "evt-uuid",
    "payload": {},
    "source": "agent | terminal-id",
    "tags": [],
    "timestamp": 1234567890,
    "type": "...",
    "workspace": "/path"
  }
]
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:751`
- Handler: `packages/core/src/events/store.ts:queryEvents (via case 'event_query')`

**Common pitfalls:**

- Returns the *last* `limit` matching events (default 50). Older events are silently dropped from the result, not paginated.
- Reads the events file fresh on every call — large logs = slow query. There is no index.
- Returns `[]` on missing file — does not surface ENOENT as an error.

---

## zana_get_profile

**Description:** Get the full configuration of a specific profile.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| profileId | string | yes | Profile ID to retrieve |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:198`

---

## zana_get_skill

**Description:** Get a specific Skill by ID.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| skillId | string | yes | Skill ID to retrieve |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:255`

---

## zana_get_team

**Description:** Get full configuration of a specific team.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| teamId | string | yes | Team ID |

**Output shape:**

```json
{
  "autoStart": false,
  "createdAt": "ISO8601",
  "description": "...",
  "dynamicSpawning": false,
  "icon": "🏗️",
  "id": "team-engineering",
  "initialPrompt": "",
  "maxTotalWorkers": 4,
  "name": "Engineering Squad",
  "orchestratorProfileId": "orchestrator",
  "rules": {
    "autoRestart": false,
    "maxConcurrentWorkers": 4,
    "requireApproval": false
  },
  "slots": [
    {
      "profileId": "coder",
      "quantity": 2
    }
  ],
  "updatedAt": "ISO8601",
  "workerProfileIds": [
    "coder",
    "tester"
  ]
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:517`
- Handler: `packages/work/src/teams/store.ts:getTeam`

**Common pitfalls:**

- Returns the team object directly, or `{ error: 'team not found: <id>' }` on miss.

---

## zana_kill_agent

**Description:** Kill/terminate a running agent.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| agentId | string | yes | Agent ID to kill |

**Output shape:**

```json
{
  "ok": true
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:182`
- Handler: `packages/core/src/agents/manager.ts case 'kill_agent'`

---

## zana_list_agents

**Description:** List all currently running agents with their status.

**Input:**

_No input parameters._

**Output shape:**

```json
[
  {
    "id": "agent-uuid",
    "lastAction": "...",
    "mode": "headless",
    "profile": "coder",
    "state": "active"
  }
]
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:155`
- Handler: `packages/core/src/agents/manager.ts case 'list_agents'`

**Common pitfalls:**

- `state` is one of `active | terminated | errored`. The list is shaped down to five fields — use `zana_agent_status` for richer detail.

---

## zana_list_channels

**Description:** List all active channels with subscriber counts and last activity.

**Input:**

_No input parameters._

**Output shape:**

```json
[
  {
    "lastActivity": 1234567890,
    "messageCount": 12,
    "name": "my-channel",
    "subscribers": 4
  }
]
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:856`
- Handler: `packages/swarm/src/swarm/router.ts:listChannels (via case 'list_channels')`

**Common pitfalls:**

- Returns a flat array. `lastActivity` is `null` if the channel was created but never published to.
- Counts are for the local in-memory router; sub-daemons' channels are not included.

---

## zana_list_profiles

**Description:** List all available agent profiles that can be spawned.

**Input:**

_No input parameters._

**Output shape:**

```json
[
  {
    "category": "core",
    "description": "...",
    "icon": "💻",
    "id": "coder",
    "model": "opus",
    "name": "Coder"
  }
]
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:193`
- Handler: `packages/core/src/agents/manager.ts case 'list_profiles'`

---

## zana_list_running_teams

**Description:** List all currently running teams with their statuses.

**Input:**

_No input parameters._

**Output shape:**

```json
[
  {
    "checkpointId": "ckpt-uuid",
    "orchestrator": {
      "...": "...",
      "id": "agent-uuid",
      "state": "active"
    },
    "orchestratorAgentId": "agent-uuid",
    "startedAt": 1234567890,
    "status": "running",
    "teamIcon": "🏗️",
    "teamId": "team-engineering",
    "teamName": "Engineering Squad",
    "workers": []
  }
]
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:617`
- Handler: `packages/work/src/teams/manager.ts:listRunningTeams`

---

## zana_list_skills

**Description:** List all Skills (shared instructions and tools injected into all agents).

**Input:**

_No input parameters._

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:250`

---

## zana_list_teams

**Description:** List all configured team templates (name, orchestrator profile, worker profiles, slot counts).

**Input:**

_No input parameters._

**Output shape:**

```json
[
  {
    "autoStart": false,
    "createdAt": "ISO8601",
    "description": "...",
    "dynamicSpawning": false,
    "icon": "🏗️",
    "id": "team-engineering",
    "initialPrompt": "",
    "maxTotalWorkers": 4,
    "name": "Engineering Squad",
    "orchestratorProfileId": "orchestrator",
    "rules": {
      "autoRestart": false,
      "maxConcurrentWorkers": 4,
      "requireApproval": false
    },
    "slots": [
      {
        "profileId": "coder",
        "quantity": 2
      }
    ],
    "updatedAt": "ISO8601",
    "workerProfileIds": [
      "coder",
      "tester"
    ]
  }
]
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:512`
- Handler: `packages/work/src/teams/store.ts:listTeams`

---

## zana_memory_search

**Description:** Search the daemon vector memory for relevant memories

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| limit | number | no | Max results (default 5) |
| query | string | yes | Search query |
| tags | array<string> | no | Filter by tags |

**Output shape:**

```json
[
  {
    "content": "...",
    "id": "uuid",
    "metadata": {
      "tags": [
        "..."
      ],
      "timestamp": 1234567890
    },
    "score": 0.87,
    "tier": "episodic"
  }
]
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:915`
- Handler: `packages/intelligence/src/intelligence/vector-memory.ts:search`

**Common pitfalls:**

- Returns a flat array of result objects. Each row has `{ id, content, metadata, score, tier }` — there is no `namespace` or `key` field.
- Score is cosine similarity (0..1). Default `minScore` is 0.1; results below that threshold are filtered out before slicing to `limit`.

---

## zana_memory_store

**Description:** Store a memory/fact in the daemon vector memory for later retrieval

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| content | string | yes | The content to remember |
| metadata | object | no | Optional metadata |
| tags | array<string> | no | Tags for categorization |

**Output shape:**

```json
{
  "id": "uuid",
  "tier": "episodic"
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:902`
- Handler: `packages/intelligence/src/intelligence/vector-memory.ts:store`

**Common pitfalls:**

- Returns `{ id, tier }` only — there is no namespace/key model. Embed identity in `metadata` (e.g. `metadata.tags`) when you need to search-by-key.

---

## zana_module_config_get

**Description:** Get configuration for a specific module

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| moduleId | string | yes | Module ID to get config for |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:951`

---

## zana_module_config_list

**Description:** List all module configurations

**Input:**

_No input parameters._

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:946`

---

## zana_module_config_set

**Description:** Set a configuration value for a specific module

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| key | string | yes | Configuration key |
| moduleId | string | yes | Module ID to configure |
| value | string | yes | Configuration value |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:962`

---

## zana_oneshot_query

**Description:** Run a quick one-shot query using a profile. Returns the text response directly. Much cheaper than spawning a full agent session — use for lookups, summaries, quick questions.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| profileId | string | yes | Profile ID to use |
| prompt | string | yes | The question or task |
| timeout | number | no | Timeout in ms (default 60000) |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:142`

---

## zana_plan_create

**Description:** Create a goal-oriented action plan (GOAP) for a complex task

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| constraints | array<string> | no | Constraints to respect |
| currentState | object | no | Current world state |
| goal | string | yes | The goal to achieve |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:928`

---

## zana_publish_channel

**Description:** Publish a message to a named channel. All subscribed agents receive it in their inbox.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| channel | string | yes | Channel name (e.g. 'findings', 'blockers', 'progress') |
| payload | object | yes | fields: content, data, kind, paths |
| type | string | yes | `finding` \| `status` \| `request` — Message type |

**Output shape:**

```json
{
  "delivered": 3,
  "ok": true,
  "subscribers": 4
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:823`
- Handler: `packages/swarm/src/swarm/router.ts:publishToChannel (via case 'publish_channel')`

**Common pitfalls:**

- `delivered` is the count of subscribers reached *excluding* the sender; `subscribers` is the total subscriber count. They differ when the publisher is also subscribed.
- Channel is created on first publish — there is no separate create call.
- History is capped at `MAX_CHANNEL_HISTORY` (in-memory ring buffer); old messages are evicted silently.

---

## zana_route_task

**Description:** Route a task to the best-fit agent profile based on content analysis

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| context | string | no | Optional context about the task |
| prompt | string | yes | The task description to route |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:890`

---

## zana_save_profile

**Description:** Create or update an agent profile. Provide an id to update, omit for new. Fields: displayName, description, icon, category, model, systemPrompt, appendSystemPrompt, permissionMode, allowedTools, disallowedTools, maxBudgetUsd, effortLevel, defaultCwd.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| profile | object | yes | Profile object. Include 'id' to update existing, omit for new. — fields: allowedTools, appendSystemPrompt, category, defaultCwd, description, disallowedTools, displayName, effortLevel, icon, id, maxBudgetUsd, model, permissionMode, systemPrompt |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:209`

---

## zana_save_skill

**Description:** Create or update a Skill. For instruction type: provide content (markdown text injected into agent system prompts). For tool type: provide toolSchema and handler.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| skill | object | yes | Skill object. Include 'id' to update existing, omit for new. — fields: content, description, enabled, handler, id, name, toolSchema, type |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:266`

---

## zana_save_team

**Description:** Create or update a team template. Provide an id to update, omit for new. Fields: name, icon, description, orchestratorProfileId, slots ([{profileId, quantity}]), initialPrompt, rules, autoStart, dynamicSpawning, maxTotalWorkers.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| team | object | yes | Team template object. Include 'id' to update existing, omit for new. — fields: autoStart, description, dynamicSpawning, icon, id, initialPrompt, maxTotalWorkers, name, orchestratorProfileId, rules, slots |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:557`

---

## zana_schedule_create

**Description:** Create a scheduled recurring action. New schedules are persisted as YAML in <workspace>/.zana/scheduler/<id>.yml. Supports a 5-field cron expression OR an intervalMs OR an `every` shorthand (e.g. '5m', '1h', '2d'). Cron schedules fire via node-cron in the daemon and survive restarts.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| action | object | yes | Action to execute when the schedule fires — fields: command, cwd, profileId, prompt, teamId, toolArgs, toolName, type |
| cron | string | no | 5-field cron expression (min hour dom mon dow). Takes precedence over intervalMs/every. |
| description | string | no |  |
| enabled | boolean | no |  |
| every | string | no | Shorthand interval, e.g. '5m', '1h', '30s', '2d'. Resolved to intervalMs internally. |
| intervalMs | number | no | Simple interval in milliseconds (alternative to cron) |
| name | string | yes | Name for this schedule |

**Output shape:**

```json
{
  "action": {
    "...": "...",
    "type": "prompt | spawn-agent | team | command | workflow | mcp_tool"
  },
  "createdAt": "ISO8601",
  "description": "",
  "enabled": true,
  "id": "uuid",
  "name": "...",
  "ownerId": null,
  "ownerName": null,
  "schedule": {
    "cron": "* * * * *",
    "every": "5m",
    "intervalMs": 300000
  },
  "status": {
    "lastRunAt": null,
    "lastRunResult": null,
    "nextRunAt": "ISO8601",
    "runCount": 0
  },
  "updatedAt": "ISO8601"
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:626`
- Handler: `packages/work/src/scheduling/service.ts:createSchedule`

**Common pitfalls:**

- Returns the schedule object directly. On invalid input returns `{ error: 'invalid schedule: <reason>' }`.
- There is no `runId` field — schedules don't fire on creation.

---

## zana_schedule_delete

**Description:** Delete a schedule.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| scheduleId | string | yes |  |

**Output shape:**

```json
{
  "ok": true
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:690`
- Handler: `packages/core/src/agents/manager.ts case 'schedule_delete'`

---

## zana_schedule_disable

**Description:** Disable a schedule without deleting it.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| scheduleId | string | yes |  |

**Output shape:**

```json
{
  "ok": true,
  "schedule": {
    "action": {
      "...": "...",
      "type": "prompt | spawn-agent | team | command | workflow | mcp_tool"
    },
    "createdAt": "ISO8601",
    "description": "",
    "enabled": true,
    "id": "uuid",
    "name": "...",
    "ownerId": null,
    "ownerName": null,
    "schedule": {
      "cron": "* * * * *",
      "every": "5m",
      "intervalMs": 300000
    },
    "status": {
      "lastRunAt": null,
      "lastRunResult": null,
      "nextRunAt": "ISO8601",
      "runCount": 0
    },
    "updatedAt": "ISO8601"
  }
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:710`
- Handler: `packages/work/src/scheduling/service.ts:disableSchedule`

---

## zana_schedule_enable

**Description:** Enable a disabled schedule.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| scheduleId | string | yes |  |

**Output shape:**

```json
{
  "ok": true,
  "schedule": {
    "action": {
      "...": "...",
      "type": "prompt | spawn-agent | team | command | workflow | mcp_tool"
    },
    "createdAt": "ISO8601",
    "description": "",
    "enabled": true,
    "id": "uuid",
    "name": "...",
    "ownerId": null,
    "ownerName": null,
    "schedule": {
      "cron": "* * * * *",
      "every": "5m",
      "intervalMs": 300000
    },
    "status": {
      "lastRunAt": null,
      "lastRunResult": null,
      "nextRunAt": "ISO8601",
      "runCount": 0
    },
    "updatedAt": "ISO8601"
  }
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:701`
- Handler: `packages/work/src/scheduling/service.ts:enableSchedule`

---

## zana_schedule_get

**Description:** Get a schedule and its recent run history.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| scheduleId | string | yes |  |

**Output shape:**

```json
{
  "history": [
    {
      "...": "...",
      "actionType": "prompt",
      "finishedAt": "ISO8601",
      "startedAt": "ISO8601",
      "status": "success"
    }
  ],
  "schedule": {
    "action": {
      "...": "...",
      "type": "prompt | spawn-agent | team | command | workflow | mcp_tool"
    },
    "createdAt": "ISO8601",
    "description": "",
    "enabled": true,
    "id": "uuid",
    "name": "...",
    "ownerId": null,
    "ownerName": null,
    "schedule": {
      "cron": "* * * * *",
      "every": "5m",
      "intervalMs": 300000
    },
    "status": {
      "lastRunAt": null,
      "lastRunResult": null,
      "nextRunAt": "ISO8601",
      "runCount": 0
    },
    "updatedAt": "ISO8601"
  }
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:663`
- Handler: `packages/core/src/agents/manager.ts case 'schedule_get'`

**Common pitfalls:**

- The MCP wrapper bundles the schedule and its run history into one envelope `{ schedule, history }` — the underlying `getSchedule` returns just the schedule.

---

## zana_schedule_list

**Description:** List all scheduled actions.

**Input:**

_No input parameters._

**Output shape:**

```json
[
  {
    "action": {
      "...": "...",
      "type": "prompt | spawn-agent | team | command | workflow | mcp_tool"
    },
    "createdAt": "ISO8601",
    "description": "",
    "enabled": true,
    "id": "uuid",
    "name": "...",
    "ownerId": null,
    "ownerName": null,
    "schedule": {
      "cron": "* * * * *",
      "every": "5m",
      "intervalMs": 300000
    },
    "status": {
      "lastRunAt": null,
      "lastRunResult": null,
      "nextRunAt": "ISO8601",
      "runCount": 0
    },
    "updatedAt": "ISO8601"
  }
]
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:658`
- Handler: `packages/work/src/scheduling/store.ts:listSchedules`

---

## zana_schedule_reload

**Description:** Re-read all <workspace>/.zana/scheduler/*.{yml,json} files from disk and re-register triggers for the enabled ones. Use after hand-editing schedule YAML files so the daemon picks up the changes without a restart. Idempotent.

**Input:**

_No input parameters._

**Output shape:**

```json
{
  "ok": true,
  "skipped": 0,
  "started": 3
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:728`
- Handler: `packages/work/src/scheduling/service.ts:loadFromDisk`

---

## zana_schedule_trigger

**Description:** Manually trigger a schedule to run immediately.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| scheduleId | string | yes |  |

**Output shape:**

```json
{
  "ok": true,
  "result": {
    "actionType": "prompt",
    "agentId": "agent-uuid",
    "finalStatus": "pending",
    "finishedAt": "ISO8601",
    "startedAt": "ISO8601",
    "status": "success",
    "summary": ""
  },
  "schedule": {
    "action": {
      "...": "...",
      "type": "prompt | spawn-agent | team | command | workflow | mcp_tool"
    },
    "createdAt": "ISO8601",
    "description": "",
    "enabled": true,
    "id": "uuid",
    "name": "...",
    "ownerId": null,
    "ownerName": null,
    "schedule": {
      "cron": "* * * * *",
      "every": "5m",
      "intervalMs": 300000
    },
    "status": {
      "lastRunAt": null,
      "lastRunResult": null,
      "nextRunAt": "ISO8601",
      "runCount": 0
    },
    "updatedAt": "ISO8601"
  }
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:719`
- Handler: `packages/work/src/scheduling/service.ts:triggerSchedule`

**Common pitfalls:**

- There is NO top-level `runId` field. Output is `{ ok, schedule, result }`. The `result` object's keys depend on the action type — common keys are `status`, `startedAt`, `finishedAt`, `actionType`, plus action-specific fields like `agentId`, `runId` (workflows only), `stdout/stderr` (commands), `data` (mcp_tool).

---

## zana_schedule_update

**Description:** Update a schedule's configuration.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| action | object | no |  |
| cron | string | no |  |
| enabled | boolean | no |  |
| intervalMs | number | no |  |
| name | string | no |  |
| scheduleId | string | yes |  |

**Output shape:**

```json
{
  "ok": true,
  "schedule": {
    "action": {
      "...": "...",
      "type": "prompt | spawn-agent | team | command | workflow | mcp_tool"
    },
    "createdAt": "ISO8601",
    "description": "",
    "enabled": true,
    "id": "uuid",
    "name": "...",
    "ownerId": null,
    "ownerName": null,
    "schedule": {
      "cron": "* * * * *",
      "every": "5m",
      "intervalMs": 300000
    },
    "status": {
      "lastRunAt": null,
      "lastRunResult": null,
      "nextRunAt": "ISO8601",
      "runCount": 0
    },
    "updatedAt": "ISO8601"
  }
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:674`
- Handler: `packages/work/src/scheduling/service.ts:updateSchedule`

---

## zana_send_ack

**Description:** Acknowledge receipt/processing of a message that requested acknowledgment.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| messageId | string | yes | ID of the message to acknowledge |
| response | string | no | Optional response text |
| status | string | yes | `received` \| `processing` \| `completed` — Acknowledgment status |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:873`

---

## zana_send_message

**Description:** Send a typed message to another agent. Supports various message types for structured communication.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| payload | object | yes | Message payload — fields: content, data, kind, paths, ticketId |
| priority | string | no | `low` \| `normal` \| `urgent` — Message priority (default: normal) |
| replyTo | string | no | Message ID if this is a reply |
| requiresAck | boolean | no | Whether to request acknowledgment |
| toAgentId | string | yes | Target agent ID |
| type | string | yes | `question` \| `finding` \| `handoff` \| `status` \| `request` — Message type |

**Output shape:**

```json
{
  "delivered": "local | remote | failed",
  "error": "<set when ok=false, e.g. 'invalid message type: X' or 'target agent not found in any daemon'>",
  "messageId": "msg-uuid",
  "ok": true
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:796`
- Handler: `packages/swarm/src/swarm/router.ts:routeMessage (via packages/core/src/agents/manager.ts case 'send_message')`

**Common pitfalls:**

- Three branches: local agent (`{ ok: true, delivered: 'local', messageId }`), remote sub-daemon (`{ ok: true|false, delivered: 'remote'|'failed', messageId }`), or unreachable (`{ ok: false, error: 'target agent not found in any daemon', messageId }`).
- Validation failure on `type` returns early without `messageId` and without routing — render `error` first.
- If `requiresAck` was set, the ack lands later via `zana_send_ack`; the send response itself does not wait.

---

## zana_spawn_agent

**Description:** Spawn a sub-agent from an available profile. The agent runs in headless mode and works on the current project.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| profileId | string | yes | Profile ID to spawn (use zana_list_profiles to see available) |
| prompt | string | yes | Initial task/prompt to give the sub-agent |

**Output shape:**

```json
{
  "agentId": "agent-uuid",
  "status": "spawned"
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:102`
- Handler: `packages/core/src/agents/manager.ts case 'spawn_agent'`

**Common pitfalls:**

- Resource and circuit-breaker guards return `{ error: '...' }` instead of throwing — always check for an `error` key before using `agentId`.

---

## zana_spawn_agent_validated

**Description:** Spawn a sub-agent with output validation guardrails. The agent's output is checked against the specified validators and retried automatically if validation fails.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| guardrails | array<object> | yes | List of guardrail configs to validate the agent's output |
| maxRetries | number | no | Maximum retry attempts on validation failure (default: 2) |
| profileId | string | yes | Profile ID to spawn |
| prompt | string | yes | Initial task/prompt for the agent |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:114`

---

## zana_sprint_board

**Description:** Get a sprint board with tickets grouped by status columns (backlog, in-progress, review, done).

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| sprintId | string | yes | Sprint ID |

**Output shape:**

```json
{
  "backlog": [
    {
      "assigneeId": null,
      "assigneeName": null,
      "assigneeProfileId": null,
      "audit": [
        {
          "action": "created",
          "actor": "agent",
          "details": {
            "priority": "medium",
            "title": "..."
          },
          "id": "uuid",
          "timestamp": "ISO8601"
        }
      ],
      "blockedBy": [],
      "closedAt": null,
      "comments": [],
      "createdAt": "ISO8601",
      "createdBy": "agent",
      "description": "...",
      "id": "uuid",
      "labels": [],
      "priority": "medium",
      "resultSummary": null,
      "reviewPhase": null,
      "reworkCount": 0,
      "sprintId": null,
      "status": "backlog",
      "title": "...",
      "updatedAt": "ISO8601"
    }
  ],
  "blocked": [],
  "done": [],
  "in-progress": [],
  "review": [],
  "rework": []
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:462`
- Handler: `packages/work/src/tickets/service.ts:getSprintBoard`

**Common pitfalls:**

- Board has SIX columns, not the four the schema description hints at: `backlog | in-progress | review | rework | blocked | done`. `cancelled` tickets are dropped.

---

## zana_sprint_create

**Description:** Create a new sprint to organize tickets.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| name | string | yes | Sprint name |
| teamId | string | no | Team this sprint belongs to |
| ticketIds | array<string> | no | Tickets to include |

**Output shape:**

```json
{
  "createdAt": "ISO8601",
  "daemonId": null,
  "endedAt": null,
  "id": "uuid",
  "name": "...",
  "startedAt": null,
  "status": "planning",
  "teamId": null,
  "ticketIds": [],
  "updatedAt": "ISO8601"
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:473`
- Handler: `packages/work/src/tickets/service.ts:createSprint`

**Common pitfalls:**

- Returns the sprint object directly (no `{ ok, sprint }` wrapper).
- Initial status is `planning` — call `zana_sprint_start` to move to `active`.

---

## zana_sprint_end

**Description:** End an active sprint.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| sprintId | string | yes |  |

**Output shape:**

```json
{
  "ok": true,
  "sprint": {
    "createdAt": "ISO8601",
    "daemonId": null,
    "endedAt": null,
    "id": "uuid",
    "name": "...",
    "startedAt": null,
    "status": "planning",
    "teamId": null,
    "ticketIds": [],
    "updatedAt": "ISO8601"
  }
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:497`
- Handler: `packages/work/src/tickets/service.ts:endSprint`

---

## zana_sprint_list

**Description:** List sprints, optionally filtered by team or status.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| status | string | no | `planning` \| `active` \| `completed` |
| teamId | string | no |  |

**Output shape:**

```json
[
  {
    "createdAt": "ISO8601",
    "daemonId": null,
    "endedAt": null,
    "id": "uuid",
    "name": "...",
    "startedAt": null,
    "status": "planning",
    "teamId": null,
    "ticketIds": [],
    "updatedAt": "ISO8601"
  }
]
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:451`
- Handler: `packages/work/src/tickets/db.ts:_listSprints`

---

## zana_sprint_start

**Description:** Start a sprint (moves from planning to active).

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| sprintId | string | yes |  |

**Output shape:**

```json
{
  "ok": true,
  "sprint": {
    "createdAt": "ISO8601",
    "daemonId": null,
    "endedAt": null,
    "id": "uuid",
    "name": "...",
    "startedAt": null,
    "status": "planning",
    "teamId": null,
    "ticketIds": [],
    "updatedAt": "ISO8601"
  }
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:486`
- Handler: `packages/work/src/tickets/service.ts:startSprint`

---

## zana_start_team

**Description:** Start a team — spawn the orchestrator + workers per the team's slot config. Returns the run ID.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| cwd | string | no | Working directory (defaults to current workspace) |
| prompt | string | yes | Initial task/prompt for the orchestrator |
| teamId | string | yes | Team ID to start |

**Output shape:**

```json
{
  "ok": true,
  "orchestratorAgentId": "agent-uuid",
  "terminalId": "terminal-uuid"
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:526`
- Handler: `packages/work/src/teams/manager.ts:startTeam`

**Common pitfalls:**

- There is NO `runId` field. The shape is exactly `{ ok, orchestratorAgentId, terminalId }`. `terminalId` is `undefined` for headless spawns.
- Failure cases return `{ ok: false, error }` — `ok` is the discriminator, not presence of `error`.

---

## zana_stop_team

**Description:** Stop a running team — kills the orchestrator and all workers.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| teamId | string | yes | Team ID to stop |

**Output shape:**

```json
{
  "ok": true
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:539`
- Handler: `packages/work/src/teams/manager.ts:stopTeam`

**Common pitfalls:**

- On miss returns `{ ok: false, error: 'team not running' }`.

---

## zana_subscribe_channel

**Description:** Subscribe to a named channel to receive messages published to it.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| channel | string | yes | Channel name to subscribe to |

**Output shape:**

```json
{
  "channel": "my-channel",
  "historyCount": 12,
  "ok": true
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:845`
- Handler: `packages/swarm/src/swarm/router.ts:subscribeChannel (via case 'subscribe_channel')`

**Common pitfalls:**

- Idempotent — re-subscribing the same agentId is a no-op and still returns `ok: true`.
- `historyCount` is messages currently in the channel ring buffer, not unread count.
- Subscribes the caller's `callerAgentId` (resolved from env), not an arbitrary id.

---

## zana_swarm_broadcast

**Description:** Send a message to ALL running child daemons. Each team lead receives the instruction.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| message | string | yes | Message to broadcast to all child daemon team leads |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1174`

---

## zana_swarm_instruct

**Description:** Send an instruction to a child daemon's team lead. Instructions flow DOWN only (master → team lead → workers).

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| daemonId | string | yes | Child daemon ID to instruct |
| message | string | yes | Instruction message for the team lead |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1151`

---

## zana_swarm_list

**Description:** List all spawned child daemons with their status, ports, and team info.

**Input:**

_No input parameters._

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1146`

---

## zana_swarm_poll_events

**Description:** Poll events from child daemons (progress reports, decision requests, completions, errors).

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| since | number | no | Timestamp (ms) — only return events after this time. Omit for all. |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1185`

---

## zana_swarm_spawn

**Description:** Spawn a new child daemon (headless team). The child daemon runs as a child process and can be controlled via instructions.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| prompt | string | no | Initial prompt/task for the child daemon |
| teamId | string | no | Team ID to start in the child daemon (optional — omit to spawn a single orchestrator) |
| workspace | string | no | Working directory for the child daemon (defaults to current workspace) |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1134`

---

## zana_swarm_stop

**Description:** Stop a child daemon. Sends SIGTERM to the child process.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| daemonId | string | yes | Child daemon ID to stop |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1163`

---

## zana_team_status

**Description:** Get status of a running team — orchestrator state, worker states, run ID.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| teamId | string | yes | Team ID |

**Output shape:**

```json
{
  "checkpointId": "ckpt-uuid",
  "checkpointedAgents": "<Set>",
  "orchestrator": {
    "...": "...",
    "id": "agent-uuid",
    "state": "active"
  },
  "orchestratorAgentId": "agent-uuid",
  "startedAt": 1234567890,
  "status": "running",
  "teamIcon": "🏗️",
  "teamId": "team-engineering",
  "teamName": "Engineering Squad",
  "workers": [
    {
      "...": "...",
      "id": "agent-uuid",
      "parentAgentId": "agent-uuid",
      "state": "active"
    }
  ]
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:548`
- Handler: `packages/work/src/teams/manager.ts:getTeamStatus`

**Common pitfalls:**

- When the team is not running the orchestrator wraps null into `{ error: 'team not running: <id>' }`. Otherwise the running record is spread alongside `orchestrator` and `workers`.

---

## zana_ticket_add_to_sprint

**Description:** Add a ticket to a sprint.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| sprintId | string | yes |  |
| ticketId | string | yes |  |

**Output shape:**

```json
{
  "ok": true,
  "sprint": {
    "createdAt": "ISO8601",
    "daemonId": null,
    "endedAt": null,
    "id": "uuid",
    "name": "...",
    "startedAt": null,
    "status": "planning",
    "teamId": null,
    "ticketIds": [],
    "updatedAt": "ISO8601"
  },
  "ticket": {
    "assigneeId": null,
    "assigneeName": null,
    "assigneeProfileId": null,
    "audit": [
      {
        "action": "created",
        "actor": "agent",
        "details": {
          "priority": "medium",
          "title": "..."
        },
        "id": "uuid",
        "timestamp": "ISO8601"
      }
    ],
    "blockedBy": [],
    "closedAt": null,
    "comments": [],
    "createdAt": "ISO8601",
    "createdBy": "agent",
    "description": "...",
    "id": "uuid",
    "labels": [],
    "priority": "medium",
    "resultSummary": null,
    "reviewPhase": null,
    "reworkCount": 0,
    "sprintId": null,
    "status": "backlog",
    "title": "...",
    "updatedAt": "ISO8601"
  }
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:439`
- Handler: `packages/work/src/tickets/service.ts:addTicketToSprint`

---

## zana_ticket_claim

**Description:** Claim a ticket (assigns it to you and moves to in-progress). Works on backlog and rework tickets.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| ticketId | string | yes | Ticket ID to claim |

**Output shape:**

```json
{
  "ok": true,
  "ticket": {
    "assigneeId": null,
    "assigneeName": null,
    "assigneeProfileId": null,
    "audit": [
      {
        "action": "created",
        "actor": "agent",
        "details": {
          "priority": "medium",
          "title": "..."
        },
        "id": "uuid",
        "timestamp": "ISO8601"
      }
    ],
    "blockedBy": [],
    "closedAt": null,
    "comments": [],
    "createdAt": "ISO8601",
    "createdBy": "agent",
    "description": "...",
    "id": "uuid",
    "labels": [],
    "priority": "medium",
    "resultSummary": null,
    "reviewPhase": null,
    "reworkCount": 0,
    "sprintId": null,
    "status": "backlog",
    "title": "...",
    "updatedAt": "ISO8601"
  }
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:358`
- Handler: `packages/work/src/tickets/service.ts:claimTicket`

**Common pitfalls:**

- Only `backlog` and `rework` tickets are claimable. Anything else returns `{ error: 'cannot claim ticket in status: X' }`.
- After claim, ticket.status becomes `in-progress` and `assigneeId/assigneeName` are populated from the calling environment.

---

## zana_ticket_comment

**Description:** Add a comment/update log to a ticket.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| body | string | yes | Comment text |
| ticketId | string | yes |  |

**Output shape:**

```json
{
  "comment": {
    "authorId": "...",
    "authorName": "...",
    "body": "...",
    "createdAt": "ISO8601",
    "id": "uuid"
  },
  "ok": true
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:381`
- Handler: `packages/work/src/tickets/service.ts:addComment`

**Common pitfalls:**

- Returns just the new comment under `{ ok, comment }` — NOT the full ticket. To re-render the ticket, follow up with `zana_ticket_get`.

---

## zana_ticket_complete

**Description:** Mark a ticket as done with a result summary describing what was accomplished.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| resultSummary | string | yes | Summary of what was done |
| ticketId | string | yes |  |

**Output shape:**

```json
{
  "ok": true,
  "ticket": {
    "assigneeId": null,
    "assigneeName": null,
    "assigneeProfileId": null,
    "audit": [
      {
        "action": "created",
        "actor": "agent",
        "details": {
          "priority": "medium",
          "title": "..."
        },
        "id": "uuid",
        "timestamp": "ISO8601"
      }
    ],
    "blockedBy": [],
    "closedAt": null,
    "comments": [],
    "createdAt": "ISO8601",
    "createdBy": "agent",
    "description": "...",
    "id": "uuid",
    "labels": [],
    "priority": "medium",
    "resultSummary": null,
    "reviewPhase": null,
    "reworkCount": 0,
    "sprintId": null,
    "status": "backlog",
    "title": "...",
    "updatedAt": "ISO8601"
  }
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:393`
- Handler: `packages/work/src/tickets/service.ts:completeTicket`

**Common pitfalls:**

- Bypasses the normal STATUS_TRANSITIONS check — a ticket can be force-completed from any status.
- Sets `status: 'done'`, `closedAt`, and stamps `resultSummary`.

---

## zana_ticket_create

**Description:** Create a new ticket for tracking work. Tickets are globally shared across all daemons.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| blockedBy | array<string> | no | IDs of tickets blocking this one |
| description | string | no | Detailed description of the work |
| labels | array<string> | no | Tags/labels |
| priority | string | no | `critical` \| `high` \| `medium` \| `low` — Priority level |
| sprintId | string | no | Sprint to add this ticket to |
| title | string | yes | Short title for the ticket |

**Output shape:**

```json
{
  "assigneeId": null,
  "assigneeName": null,
  "assigneeProfileId": null,
  "audit": [
    {
      "action": "created",
      "actor": "agent",
      "details": {
        "priority": "medium",
        "title": "..."
      },
      "id": "uuid",
      "timestamp": "ISO8601"
    }
  ],
  "blockedBy": [],
  "closedAt": null,
  "comments": [],
  "createdAt": "ISO8601",
  "createdBy": "agent",
  "description": "...",
  "id": "uuid",
  "labels": [],
  "priority": "medium",
  "resultSummary": null,
  "reviewPhase": null,
  "reworkCount": 0,
  "sprintId": null,
  "status": "backlog",
  "title": "...",
  "updatedAt": "ISO8601"
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:318`
- Handler: `packages/work/src/tickets/service.ts:createTicket`

**Common pitfalls:**

- Priority enum is `critical|high|medium|low`, NOT `P0|P1|P2|P3`.
- Initial status is always `backlog` — there is no `open` status.
- Returns the ticket directly (not `{ ok, ticket }`). Errors return `{ error: '...' }`.

---

## zana_ticket_edit

**Description:** Edit a ticket's fields (title, description, priority, labels, type, sprintId). Only specified fields are updated.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| description | string | no | New description (optional) |
| labels | array<string> | no | New labels array (replaces existing) |
| priority | string | no | `critical` \| `high` \| `medium` \| `low` — New priority (optional) |
| sprintId | string | no | Move to sprint (optional) |
| ticketId | string | yes | Ticket ID to edit |
| title | string | no | New title (optional) |
| type | string | no | `bug` \| `feature` \| `chore` \| `spike` — Ticket type (optional) |

**Output shape:**

```json
{
  "ok": true,
  "ticket": {
    "assigneeId": null,
    "assigneeName": null,
    "assigneeProfileId": null,
    "audit": [
      {
        "action": "created",
        "actor": "agent",
        "details": {
          "priority": "medium",
          "title": "..."
        },
        "id": "uuid",
        "timestamp": "ISO8601"
      }
    ],
    "blockedBy": [],
    "closedAt": null,
    "comments": [],
    "createdAt": "ISO8601",
    "createdBy": "agent",
    "description": "...",
    "id": "uuid",
    "labels": [],
    "priority": "medium",
    "resultSummary": null,
    "reviewPhase": null,
    "reworkCount": 0,
    "sprintId": null,
    "status": "backlog",
    "title": "...",
    "updatedAt": "ISO8601"
  }
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:405`
- Handler: `packages/work/src/tickets/service.ts:updateTicket`

**Common pitfalls:**

- Only fields in UPDATABLE_FIELDS (`title|description|priority|labels|sprintId|blockedBy|type`) are applied. Unknown keys are silently ignored, but if NO valid fields are provided you get `{ error: 'no valid updatable fields provided' }`.
- `type` enum is `bug|feature|chore|spike`.

---

## zana_ticket_get

**Description:** Get full details of a specific ticket including comments and history.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| ticketId | string | yes | Ticket ID |

**Output shape:**

```json
{
  "assigneeId": null,
  "assigneeName": null,
  "assigneeProfileId": null,
  "audit": [
    {
      "action": "created",
      "actor": "agent",
      "details": {
        "priority": "medium",
        "title": "..."
      },
      "id": "uuid",
      "timestamp": "ISO8601"
    }
  ],
  "blockedBy": [],
  "closedAt": null,
  "comments": [],
  "createdAt": "ISO8601",
  "createdBy": "agent",
  "description": "...",
  "id": "uuid",
  "labels": [],
  "priority": "medium",
  "resultSummary": null,
  "reviewPhase": null,
  "reworkCount": 0,
  "sprintId": null,
  "status": "backlog",
  "title": "...",
  "updatedAt": "ISO8601"
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:347`
- Handler: `packages/work/src/tickets/db.ts:_getTicket`

**Common pitfalls:**

- Returns the ticket object directly, or `undefined` when the row is missing — there is no `{ error }` envelope on miss.

---

## zana_ticket_list

**Description:** List/filter tickets. All tickets are globally shared.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| assigneeId | string | no |  |
| label | string | no |  |
| sprintId | string | no |  |
| status | string | no | `backlog` \| `in-progress` \| `review` \| `done` \| `cancelled` |

**Output shape:**

```json
[
  {
    "assigneeId": null,
    "assigneeName": null,
    "assigneeProfileId": null,
    "audit": [
      {
        "action": "created",
        "actor": "agent",
        "details": {
          "priority": "medium",
          "title": "..."
        },
        "id": "uuid",
        "timestamp": "ISO8601"
      }
    ],
    "blockedBy": [],
    "closedAt": null,
    "comments": [],
    "createdAt": "ISO8601",
    "createdBy": "agent",
    "description": "...",
    "id": "uuid",
    "labels": [],
    "priority": "medium",
    "resultSummary": null,
    "reviewPhase": null,
    "reworkCount": 0,
    "sprintId": null,
    "status": "backlog",
    "title": "...",
    "updatedAt": "ISO8601"
  }
]
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:334`
- Handler: `packages/work/src/tickets/db.ts:_listTickets`

**Common pitfalls:**

- Status enum: `backlog | in-progress | review | rework | blocked | done | cancelled`. The MCP input schema lists a smaller subset for filtering, but tickets in the result set may carry any of these.
- Returns a flat array of ticket objects, sorted by `updatedAt DESC`. There is no wrapper object.

---

## zana_ticket_update

**Description:** Update a ticket with progress, plan, status, review phase, or files changed. Workers and reviewers use this to report progress and advance the review pipeline.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| filesChanged | array<string> | no | File paths modified/created |
| planification | string | no | Implementation plan (written before coding) |
| progress | string | no | Progress note or review feedback |
| resultSummary | string | no | Final result summary |
| reviewPhase | string | no | `qa` \| `architecture` — Advance the review phase (QA reviewer sets 'architecture' on pass) |
| status | string | no | `in-progress` \| `review` \| `rework` \| `blocked` \| `done` — Transition to new status |
| ticketId | string | yes | Ticket ID to update |

**Output shape:**

```json
{
  "ok": true,
  "ticket": {
    "assigneeId": null,
    "assigneeName": null,
    "assigneeProfileId": null,
    "audit": [
      {
        "action": "created",
        "actor": "agent",
        "details": {
          "priority": "medium",
          "title": "..."
        },
        "id": "uuid",
        "timestamp": "ISO8601"
      }
    ],
    "blockedBy": [],
    "closedAt": null,
    "comments": [],
    "createdAt": "ISO8601",
    "createdBy": "agent",
    "description": "...",
    "id": "uuid",
    "labels": [],
    "priority": "medium",
    "resultSummary": null,
    "reviewPhase": null,
    "reworkCount": 0,
    "sprintId": null,
    "status": "backlog",
    "title": "...",
    "updatedAt": "ISO8601"
  }
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:422`
- Handler: `packages/core/src/agents/manager.ts case 'ticket_update'`

**Common pitfalls:**

- This is a multiplexer over several service calls — `progress` adds a comment, `planification` writes plan.md, `filesChanged` merges into files-changed.json, `resultSummary` writes result.md, `reviewPhase` advances the review phase, `status` calls updateStatus or completeTicket. The return shape depends on which branch fired:
-   • If `status === 'done'` and `resultSummary` was provided → `completeTicket` shape: `{ ok, ticket }`.
-   • Else if `status` provided → `updateStatus` shape: `{ ok, ticket }`.
-   • Otherwise → `{ ok: true, ticketId }` (no ticket object).

---

## zana_ticket_update_status

**Description:** Move a ticket to a new status. Valid transitions: backlog→in-progress/cancelled, in-progress→review/done/backlog/cancelled, review→done/rework/cancelled, rework→in-progress/cancelled.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| status | string | yes | `backlog` \| `in-progress` \| `review` \| `rework` \| `blocked` \| `done` \| `cancelled` |
| ticketId | string | yes |  |

**Output shape:**

```json
{
  "ok": true,
  "ticket": {
    "assigneeId": null,
    "assigneeName": null,
    "assigneeProfileId": null,
    "audit": [
      {
        "action": "created",
        "actor": "agent",
        "details": {
          "priority": "medium",
          "title": "..."
        },
        "id": "uuid",
        "timestamp": "ISO8601"
      }
    ],
    "blockedBy": [],
    "closedAt": null,
    "comments": [],
    "createdAt": "ISO8601",
    "createdBy": "agent",
    "description": "...",
    "id": "uuid",
    "labels": [],
    "priority": "medium",
    "resultSummary": null,
    "reviewPhase": null,
    "reworkCount": 0,
    "sprintId": null,
    "status": "backlog",
    "title": "...",
    "updatedAt": "ISO8601"
  }
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:369`
- Handler: `packages/work/src/tickets/service.ts:updateStatus`

**Common pitfalls:**

- Transitions are validated against an explicit table — see STATUS_TRANSITIONS in service.ts. Invalid transitions return `{ error }`.
- Moving to `review` auto-sets `reviewPhase: 'qa'`. Moving to `rework` clears it and increments `reworkCount`.

---

## zana_toggle_skill

**Description:** Enable or disable a Skill without deleting it.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| enabled | boolean | yes | Whether the skill should be enabled |
| skillId | string | yes | Skill ID |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:302`

---

## zana_workers_list

**Description:** List all background workers and their status

**Input:**

_No input parameters._

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:941`

---

## zana_workflow_get_run

**Description:** Get details of a specific workflow run.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| runId | string | yes | Workflow run ID |

**Output shape:**

```json
{
  "completedAt": "ISO8601 | null",
  "currentStep": 0,
  "error": null,
  "id": "run-uuid",
  "skillId": "skill-id",
  "skillName": "...",
  "startedAt": "ISO8601",
  "status": "running | completed | halted | failed",
  "steps": [
    {
      "action": "...",
      "index": 0,
      "result": {},
      "status": "completed"
    }
  ],
  "triggerContext": {}
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1063`
- Handler: `packages/work/src/scheduling/workflow-engine.ts:loadRun (via case 'zana_workflow_get_run')`

**Common pitfalls:**

- Loads from disk (`<workflowsDir>/<runId>.json`) — works even after daemon restart.
- Unknown `runId` returns `{ error: 'run not found' }`, not `null`.
- Same shape as `executeWorkflow`'s return; safe to share render code with `zana_workflow_run`.

---

## zana_workflow_list_runs

**Description:** List workflow runs, optionally filtered by status.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| status | string | no | `running` \| `completed` \| `halted` \| `failed` — Filter by status |

**Output shape:** TODO — not yet documented. Read the handler before guessing field names.

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1053`

---

## zana_workflow_run

**Description:** Trigger a workflow skill by ID. Workflows orchestrate multi-step flows with conditional agent spawning, gates, and notifications.

**Input:**

| Field | Type | Required | Enum / Notes |
|---|---|---|---|
| skillId | string | yes | ID of the workflow skill to run |
| ticketId | string | no | Optional ticket ID to pass as context |

**Output shape:**

```json
{
  "completedAt": null,
  "currentStep": 0,
  "error": null,
  "id": "run-uuid",
  "skillId": "skill-id",
  "skillName": "...",
  "startedAt": "ISO8601",
  "status": "running | completed | halted | failed",
  "steps": [
    {
      "...": "...",
      "action": "spawn | gate | notify | wait",
      "index": 0,
      "result": null,
      "status": "pending | running | completed | halted"
    }
  ],
  "triggerContext": {}
}
```

**Source:**
- MCP wrapper: `packages/mcp/src/mcp-server.ts:1041`
- Handler: `packages/work/src/scheduling/workflow-engine.ts:executeWorkflow (via packages/mcp/src/mcp-server.ts case 'zana_workflow_run')`

**Common pitfalls:**

- Returns the *full run record* once the workflow completes (or halts/fails). Not async — caller blocks until the workflow terminates.
- Failure paths return short error wrappers, not the run record: `{ error: 'max_concurrent_runs' }`, `{ error: 'no_steps' }`, `{ error: 'too_many_steps' }`. Check for an `error` key before reading `id`.
- If the underlying skill is missing or not type=workflow, the MCP wrapper returns `{ error: 'workflow skill not found' }`.
- Step results are nested in `steps[i].result`. The shape varies by `action.type` — `spawn` returns `{ agentId, profileId, prompt }`; `gate` returns `{ passed: true }`; `notify` returns `{ emitted: '<eventType>' }`; `wait` returns `{ waited: <ms> }`.

---
