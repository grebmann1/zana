function init() {
  // This plugin package primarily contributes Claude commands/skills.
  // Keep runtime initialization intentionally minimal.
}

function shutdown() {
  // No runtime resources to release.
}

module.exports = {
  init,
  shutdown,
};
