{
  "name": "@zana/swarm",
  "version": "0.1.0",
  "description": "Multi-daemon swarm coordination — router, events, spawner.",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run"
  },
  "engines": { "node": ">=20.0.0" },
  "dependencies": {
    "@zana/core": "*"
  },
  "files": ["dist/"]
}
